import { ErrorNameDirective } from './error-name.directive';

describe('ErrorNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorNameDirective();
    expect(directive).toBeTruthy();
  });
});
